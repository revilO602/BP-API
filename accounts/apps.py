from django.apps import AppConfig


class AccountConfig(AppConfig):
    """
    Configuration of the account app
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'accounts'
